{
    "header": "web",
    "request": {
        "method": "account",
        "request": {
            "method": "create",
            "user_name": "{{ user_name }}",
            "passwd": "{{ passwd }}"
        }
    }
}
