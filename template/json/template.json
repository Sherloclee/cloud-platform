{
    "header": "web",
    "request": {
        "method": "instance",
        "request": {
            "method": "CVM",
            "request": {
                "user_name": "{{ user_name }}",
                "instance_id": "{{ instance_id }}",
                "OSType": "{{ os }}",
                "vcpu": {{ vcpu }},
                "memory": {{ memory }},
                "disk_size": {{ disk_size }}
            }
        }
    }
}
