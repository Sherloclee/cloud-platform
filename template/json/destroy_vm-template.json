{
    "header": "web",
    "request": {
        "method": "instance",
        "request": {
            "method": "DVM",
            "user_name": "{{ user_name }}",
            "request": {
                "user_name": "{{ user_name }}",
                "instance_id": "{{ instance_id }}"
            }
        }
    }
}
